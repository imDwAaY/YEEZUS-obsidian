### 内容须知
#Node版本差异 
前后端所用 [[Node]] 版本不一样
后端 Node 版本为<span class="bold">{node: 'v16.20.2', npm: '8.19.4'}</span>

### 实现过程
bash中cd进入API文件夹,用口令创建服务端口
```
node app.js
```
若运行成功会报出
```
server running @ http://localhost:3000
```